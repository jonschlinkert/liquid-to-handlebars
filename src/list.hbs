---
layout: default
name: "list"
---

<ul class="list">
{{#each site.posts as |post postId}}
  <li>
    <span>{{date_to_string post.date}}</span>
    <a href="{{ post.url }}">{{ post.title }}</a>
  </li>
{{/each}}
</ul>
